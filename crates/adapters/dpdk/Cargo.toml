[package]
name = "flux-engine-dpdk"
version = "0.1.0"
edition = "2021"

[dependencies]
flux-engine-core = { path = "../../engine-core" }
libc = "0.2"
anyhow = "1.0"

[target.'cfg(target_os = "linux")'.dependencies]
dpdk-sys = "0.1"
